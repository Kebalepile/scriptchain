import React from 'react'
import CurrentNode from '../blockchain/CurrentNode'

const Blockchain = () => {
    return (
        <div className="container">
            <CurrentNode />
        </div>
    )
}

export default Blockchain
